<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include partials/_header %>

  <% if (user) { %>
  <ul>
    <% for (let url in urls) { %>
    <li><%= `${url}: ${urls[url].fullURL}` %></li>
    <p>Date Created: <%= urls[url].date %></p>
    <form action="/urls/<%= url %>" method="GET">
      <input type="submit" value="Edit">
    </form>

    <form action="/urls/<%= url %>/delete" method="POST">
      <input type="submit" value="Delete">
    </form>
    <% } %>
  </ul>

  <form action="/urls/new" method="GET">
    <input type="submit" value="Create a New Short Link">
  </form>
  <% } else { %>
  <p>Please register or log in to view your URLs.</p>
  <% }; %>

</body>
</html>

